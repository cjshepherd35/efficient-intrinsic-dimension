i train mutually exclusive subsets of a weight matrix in different layers when reusing the layer. It is trained on mnist dataset and achieves very good reduction of parameters. it does not converge as quickly 
during training as a full list of unique layers but it shows this is something that definitely should be explored more.  
